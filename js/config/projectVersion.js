/*
 * Lyric Solitaire — Project Version Configuration
 *
 * Responsibility:
 * Stores application/tool version identifiers used by the UI. Keeping the
 * version in JavaScript means the artwork does not need to change for every
 * release.
 */
window.LyricSolitaireProject = {
  gameVersion: "0.1.0",
  simulatorVersion: "0.1.0",
  dataSchemaVersion: "1.0"
};
